<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiControles.GridPrincipal"
             Title="GridPrincipal">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition  />
        </Grid.ColumnDefinitions>
        
        
        <!--linha 1-->
        <Button
            Text="Formas Geometricas"
            x:Name="btn_FormasGeometricas"
            Clicked="btn_FormasGeometricas_Clicked"
            Grid.Row="1"
            Grid.Column="0"
            HeightRequest="50"
            BackgroundColor="DarkBlue"
            />
        <!--fim linha 1-->
        <!--linha 2-->
        <Button
            Text="Forms Controls"
            x:Name="btn_FormsControls"
            Clicked="btn_FormsControls_Clicked"
            Grid.Row="2"
            Grid.Column="0"
            HeightRequest="50"
            BackgroundColor="DarkBlue"
            />
        <!--fim linha 2-->

    </Grid>
</ContentPage>